# Intentionally empty: suppresses fish's default [N]/[I] vi-mode tag on the first
# line. The prompt is styled identically in every mode, so no indicator is shown.
function fish_mode_prompt --description 'Suppress the default vi-mode indicator'
end
