# bat configuration
# https://github.com/sharkdp/bat#configuration-file

# The built-in ansi theme renders through the terminal's ANSI palette, so it
# follows whichever Ghostty theme is active with no luma detection needed.
--theme="ansi"
