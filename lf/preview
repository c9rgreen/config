#!/bin/sh

# The ansi theme from bat's config renders through the terminal's ANSI
# palette, so it follows the active Ghostty theme even though bat's output
# is piped here.
bat --color=always --style=plain --paging=never --terminal-width="$2" "$1"
